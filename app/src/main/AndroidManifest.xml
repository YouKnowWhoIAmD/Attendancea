<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devarajakhil.attendance">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EditActivity"
            android:label="@string/editMain">
        </activity>
        <activity
            android:name=".AttActivity"
            android:label="@string/AttendanceTVtext">
        </activity>
        <activity android:name=".StatActivity"
            android:label="@string/statistics">
        </activity>
        <activity android:name=".InnerAct"
            android:label="bla">
        </activity>
        <activity android:name=".AttInner">

        </activity>

    </application>


</manifest>